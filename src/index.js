const folder = require("../config/folder.json");
const login = require("../config/login.json");
const search = require("../config/search.json");
const folders = require("./folder.js");
const downloader = require("./dowloader.js")

// folders.CreateFolder(folder.baseFolder);
// folders.CreateSubFolders(folder.baseFolder, search.terms);
// downloader.DownloadImage(`${folder.baseFolder}/anime`)